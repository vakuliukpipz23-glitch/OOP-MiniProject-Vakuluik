# Iteration 3 — Lab 36 Report

Опис робіт:
- Підготовлено тестову стратегію (`docs/test-strategy.md`).
- Додано інструменти тестування та документацію (`TESTING.md`, `docs/test-matrix.md`).
- Рефакторинг для seams та тестованості (контакти патрона, категорія книги, query service).
- Реалізовано збереження/відновлення стану та інтеграційні тести для persistence.

Coverage та результати:
- Юніт-тестів: 30
- Інтеграційних тестів: 8

Запуск `dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=opencover` згенерував звіт coverage (файли `coverage.opencover.xml`) у каталозі `tests/**/TestResults`.

Виправлені smells:
- Приховані сетери в сутностях (`Email/Phone`, `Category`) зроблені контрольовано змінюваними через методи.
- Репозиторії мають чіткі API для тестування (GetAll -> IReadOnlyCollection).

Залишилися ризики перед Lab 37:
- Повна перевірка конкурентного доступу до файлу persistence не виконана.
- Відсутні property-based тести для складних доменних правил.
- Потрібно додати логування для production сценаріїв.

Наступні кроки:
- Додати мінімум 20 юніт-тестів та 8 інтеграційних тестів.
- Підключити coverlet у CI і згенерувати звіти.
- Додати тести для негативних I/O сценаріїв.
